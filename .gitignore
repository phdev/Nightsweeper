# Local machine-only config (caps, secrets paths, source lists).
# Only nightsweeper.config.example.yaml is committed.
nightsweeper.config.yaml
nightsweeper.config.local.yaml

# Local SQLite ledger + WAL sidecars
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Compound-engineering machine-local config
.compound-engineering/config.local.yaml

# Per-task worktrees (created at runtime under .nightsweeper/worktrees/)
.nightsweeper/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / TypeScript / Bun
node_modules/
dist/
bun.lockb
*.tsbuildinfo

# Go (do NOT ignore /bin/ — bin/nightsweeper.mjs is the npm package entry point)
*.out

# OS / editor
.DS_Store
*.swp
.venv/

# Generated morning report
nightsweeper-report.md
